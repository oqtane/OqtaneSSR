@using OqtaneSSR.Client.Models
@using OqtaneSSR.Components.Router
@* @inject CascadingValueSource<PageState> CascadingPageState
 *@
<CascadingValue Value="@PageState">
    <SiteRouter OnStateChange="@ChangeState" />
</CascadingValue>

@* <SiteRouter OnStateChange="@ChangeState" /> *@

@code {
    private PageState PageState { get; set; }

    private void ChangeState(PageState pageState)
    {
        PageState = pageState;
        StateHasChanged();

        // CascadingPageState.NotifyChangedAsync(pageState);
    }
}